{
  "name": "sis-core",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:reset": "bun run src/scripts/reset-db.ts",
    "cli:create-admin": "bun run src/cli/admin.user.cli.ts",
    "cli:generate-jwt-secrets": "bun run src/cli/generate.jwt.secrets.cli.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "hono": "^4.12.2",
    "postgres": "^3.4.7",
    "prompts": "^2.4.2",
    "uuidv7": "^1.1.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.8.1",
    "@types/prompts": "^2.4.9",
    "drizzle-kit": "^0.31.8"
  }
}
